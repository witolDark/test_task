import { HighlightTruncatedDirective } from './highlight-truncated.directive';

describe('HighlightTruncatedDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTruncatedDirective();
    expect(directive).toBeTruthy();
  });
});
